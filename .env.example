# Optional. Override the default notifications service URL used by
# `tomato event *` commands.
# EVENTS_URL=http://localhost:4400

# Optional. Override the OAuth redirect host/port used by `tomato linear
# login` when the default (http://localhost:80) does not suit.
# OAUTH_REDIRECT_URI=http://localhost
# OAUTH_REDIRECT_PORT=80
