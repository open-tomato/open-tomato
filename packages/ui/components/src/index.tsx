// Placeholder entry — replaced by the component barrel in WS05 wave 0
// (docs/plans/poc-release/05-ui-components-port.md). Exists so the turbo gate
// (build / test / lint) runs against a tracked workspace before the port.
export const UI_COMPONENTS_PACKAGE = '@open-tomato/ui-components';
