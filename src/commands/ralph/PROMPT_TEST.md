Run this exact bash command and nothing else: echo "done: <task name>"
The task name is whatever comes after "Your current scoped task is:".
Do not read any files. Do not make any changes. Just run the echo command and exit.
