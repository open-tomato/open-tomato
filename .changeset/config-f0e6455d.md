---
"@open-tomato/config": minor
---

Add schema v2 features: platform refs ({{platform.<vendor>.*}} pass-through), infrastructure.<vendor> pot, provision object coercion, and soft-required project.owner with warnings in LoadConfigResult.

Part of Phase 8 registry cut — see plans/phase-8/.
