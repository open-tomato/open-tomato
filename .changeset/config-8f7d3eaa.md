---
"@open-tomato/config": patch
---

Re-export schema-v2 provision (ProvisionSchema, coerceProvision), platform-ref helpers (PLATFORM_REF_PATTERN, isPlatformRef, extractPlatformRefs), and the LoadConfigResult type from @open-tomato/config's package root.
