# Phase 8 — open-tomato progress notes

Iteration learnings from the Phase 8 shared-package and CLI loops have been consolidated into
skills and AGENTS.md. Add new broadly-applicable findings to the relevant destination below
rather than appending here. Reserve this file for transient iteration notes only.

## Where the findings live

- **Scaffolding a `@open-tomato/*` shared package** — package.json/tsconfig/vitest/eslint
  conventions, devDep pins, and scaffold-time TS/ESLint gotchas (TS18003 empty-src lint,
  TS6053 tsconfig `extends` suffix, TS2307 forward type imports, `noUncheckedIndexedAccess`,
  import-order churn, `expectTypeOf` type-tests), plus the "loop task already satisfied"
  workflow → `skills/shared-package-authoring/SKILL.md`.
- **Changesets + publish pipeline** — `changeset:add` uncommitted-only detection,
  `publish:dry`/`publish:local` aborting on unrelated test failures, the
  `bun packages/scripts/publish-packages.ts` escape hatch, and registry auth →
  `skills/releasing-packages/SKILL.md`.
- **cli-core `parseArgs` contract & `CliEvent` serialization constraints** →
  `packages/shared/cli-core/AGENTS.md`.
- **Ecosystem layout, `workspace:^` policy, publish workflow** → `packages/AGENTS.md`
  (already authoritative).
- **ESLint/typecheck gotchas** — `no-unused-vars` has no `argsIgnorePattern`, Node-globals,
  import ordering, `bun:*` unresolved imports, etc. → `packages/shared/eslint-config/AGENTS.md`.
- **CLI command shape, output modes, `describe` manifest, external-command discovery, plus
  editing/testing caveats** — frozen `CliContext` + `dispatch.ts` arg-slice/`Object.freeze`
  override, `CommandRegistry` NUL-byte (`\x00`) compound keys, macOS `mkdtempSync` →
  `fs.realpathSync` test comparison, Vitest's inability to `import()` OS-tmpdir paths (inject
  the importer), and skipping changesets for the `private` cli package →
  `cli/AGENTS.md`.

<!-- Scratch space for transient iteration notes. Promote anything broadly applicable to a
     destination above, then delete it from here. -->
